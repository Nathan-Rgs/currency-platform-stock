# Core API
fastapi~=0.115
uvicorn[standard]~=0.30

# DB & ORM
SQLAlchemy~=2.0
alembic~=1.13
psycopg[binary]~=3.2

# Validation & settings
pydantic~=2.9
pydantic-settings~=2.3
email-validator~=2.2

# Auth (opcional, mas recomendado)
passlib[bcrypt]~=1.7
bcrypt~=4.2
PyJWT~=2.9

# Uploads & parsing
python-multipart~=0.0.9

# Performance/qualidade
orjson~=3.10

# Env helpers (se preferir sobre pydantic-settings)
python-dotenv~=1.0

# Logging (opcional)
loguru~=0.7

# Futuro: storage em cloud (opcional)
# boto3~=1.35  # para S3
